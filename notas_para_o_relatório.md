1. LLM esqueceu de adicionar a 'node_modules/' ao .gitignore
2. LLM não colocou o "Cadastro de admin". Só dava pra cadastrar por requests HTTP, isso foi adicionado após os
    primeiros testes
3. LLM colocou a .env/ no repo
